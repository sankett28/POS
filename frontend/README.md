# Retail Boss Frontend - Next.js

This is the Next.js frontend for the Retail Boss application.

## Setup

1. Navigate to the `frontend` directory:
   `cd frontend`

2. Install dependencies:
   `npm install`

3. Start the frontend server:
   `npm run dev`

4. The frontend will automatically proxy API calls to the backend running on `http://localhost:8000`.

